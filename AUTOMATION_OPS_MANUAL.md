# ベモーレ自動化 運用マニュアル（ひとり運用の早見表）

> このドキュメント1枚で「いつ何が動くか」「通知が来たらどうするか」が分かります。
> 作成: 2026-07-08 ／ 対象リポジトリ: threads-bot / instagram-stories-bemolle / bemolle-review

## 📝 変更履歴（運用に影響する変更のみ）

**2026-08-01 GASデプロイ版ズレ監視を新設＋古い残骸デプロイを掃除**
- 「コードを保存したのに再デプロイし忘れ→本番が古いコードのまま動く」型の沈黙障害を、**毎朝9:00に自動検知**（遅れ/消失があればClaude通知BotへLINE・正常時は無音）。対象＝バージョン固定運用のGAS（うらかたStripe通知・タイムカード）。フォーム系GASはトリガー実行で常に最新＝ズレ不能のため対象外
- 8/1のうらかたStripe通知の二重送信（保存済みだが固定デプロイ未更新が原因）を修正し、Stripe/タイムカード/廃止済みベモーレ予約の**不要な古いデプロイを削除**（本番@28/@20は無傷）

**2026-07-12(夜) 通知の安全化＋★5自動返信の完了通知化＋履歴PII消去**
- ★5口コミの自動返信：LINEは**Google投稿が成功した後に1通だけ**届く方式に変更（「完了LINEが来た＝載った」。来ない場合は投稿失敗＝Makeからエラーメール）
- 障害通知のLINEを**broadcast→管理者宛てpush**に切替（とうこさんLINEの顧客に管理者アラートが見える構造を封鎖）。彩さんへの届き方は変わらない
- threads-bot公開リポの**git履歴から顧客投稿ファイル9件と実名入りコミットメッセージを完全消去**（filter-repo・現ツリーは7/12昼に対応済みだった分の履歴側）

**2026-07-11(夜) 月次総点検の仕組みを新設＋監査指摘13件を全対応**
- **毎月1日 朝9時すぎ**にクラウドのレビューAIが全自動化リポを点検→結果がLINEに届く（🟢なら何もしなくてOK）。Macオフでも動く
- レポートが来なかった場合は**毎月2日 昼12時**に見張り番が🚨をLINEに送る（覚えておく必要なし）
- Square課金監視を強化：**毎月2日 0時/6時/12時**の3回体制（成功すれば1通だけ）。決済失敗→自動解約のケースも🚨要対応として表示されるように
- GBP自動投稿はMakeが**実際に投稿を完了してから「posted」と返す**方式に変更。失敗時だけ⚠️LINEが届く（月1確認は不要）
- タイムアウトで強制終了したジョブの失敗通知が飛ばない穴を全リポで修正（実機検証済み）
- square-readerリポジトリを非公開(private)に変更（顧客名の露出対策）

**2026-07-11 全リポ一斉総点検（二重投稿・沈黙障害・LINE枠の穴を一括修正）**
- 障害のLINE通知は**1障害につき1通**に変更（Python側が通知したらworkflow側は重複送信しない）
- Threads「投稿なし」の日のℹ️通知は**1日1回だけ**届く（以前は朝の予備cronごとに最大3〜4通）
- SaaS投稿でクライアント1件が失敗しても、ベモーレ/個人の投稿記録は正しく保存される（heartbeatの誤った再投稿=二重投稿を防止）
- comment.yml（自動コメント/クラウド）はcron発火を廃止（実体はローカルMac実行のため空回りしていた）。手動実行は引き続き可能
- GBP週次投稿・各ストーリーの二重投稿防止を強化（古いコード取得の穴・push失敗の握りつぶしを封鎖）
- Meta広告のトークン自動更新に「本当に延長されたか」の検証を追加。ヘルスチェックは週1回・LINE通知化

---

## 0. まず知っておくこと（全体像）

あなたの自動化は **GitHub Actions（クラウド）** で動いています。あなたのPCは基本オフでもOK。
例外は **ローカルMacのcookie_refresh**（Threadsのログイン情報を自動更新）だけ、Macが起きている必要があります。

- **主系（メイン）**: cron-job.org が時刻ちょうどに発火（GitHubのスケジュールは遅れやすいため）
- **予備（バックアップ）**: GitHub Actions のスケジュールが少し後に発火（主系が失敗しても投稿されるように）
- **二重投稿の防止**: 「今日もう投稿したか」を記録して、主系＋予備が両方動いても投稿は1回だけ

---

## 1. スケジュール表（毎日/毎週 何が動くか・JST）

### 毎日
| 時刻(JST) | 何が動く | 内容 |
|---|---|---|
| 07:00 | IGストーリー投稿 | @bemolle_diet のストーリー自動投稿 |
| 07:30 | Threads朝投稿 | SaaS各アカ・ベモーレ・個人の朝の投稿 |
| 08:00〜09:00 | Threads→ストーリー | Threadsの朝投稿をIGストーリーにも展開 |
| 09:00 | 個人リマインダー | reminders.json の当日分をClaude通知BotでLINEに送信（公庫=毎月27日など・保険発火9:40・IGリポジトリ） |
| 09:00 | GASデプロイ版ズレ監視 | GASの本番デプロイが最新版か点検・ズレたらLINE（正常時は無音・IGリポジトリ） |
| 10:00 | OAuthリマインダー | SaaSクライアントの連携未完了を通知 |
| 11:00 | ハートビート① | 朝投稿が成功したか確認・失敗なら再投稿 |
| 12:30 | Threads昼投稿 | 各アカの昼の投稿 |
| 16:00 | ハートビート② | 昼投稿の成功確認 |
| 3回/日 | 自動いいね | ベモーレ・個人（cron-job.org経由・朝昼夜） |
| 21:30 | Threads夜投稿 | 各アカの夜の投稿 |
| 23:00 | ハートビート③ | 夜投稿の成功確認 |
| 12:00, 19:00 | Cookie自動更新(Mac) | Threadsのログイン情報を更新（launchd） |

### 毎週
| 曜日・時刻(JST) | 何が動く | 内容 |
|---|---|---|
| 日 08:30 | GBP投稿 | Googleビジネスプロフィールの週次投稿（Make経由） |
| 日 10:00 | Threadsトークン更新 | Threads APIトークンの週次リフレッシュ |
| 月 10:00 | 週次リサーチ | 反響投稿を分析してルール更新 |
| 月 11:00 | SaaS投稿生成 | クライアント各サロンの投稿を生成・補充 |
| 水 08:30 | GBP投稿 | 週2回目のGBP投稿 |
| 月・木 07:30 / 04:00 | Cookie更新(Mac) | 追加のCookie更新スケジュール |

### 毎月
| タイミング | 何が動く |
|---|---|
| 1日 09:05 | **月次総点検**（クラウドAIが全リポをレビュー→結果をLINEへ） |
| 2日 00:00 / 06:00 / 12:00 | Square課金監視（本命＋予備2回・サマリーは1通だけ） |
| 2日 12:00 | 総点検の見張り番（当月レポートが無ければ🚨LINE） |
| 月末(28-31) 09:00 | Anthropic API 月次レポート |

### 常時（自動・状態依存）
- **投稿プールの自動補充**: 各スロットの投稿ストックが5本以下になったら12本自動生成
- **ターゲット自動発掘**: いいね対象が50件以下になったらキーワード検索で補充

---

## 2. 各自動化の役割（何のためのものか）

### 📱 SNS投稿
| 名前 | リポジトリ | 役割 |
|---|---|---|
| daily_story | instagram-stories | 毎朝IGストーリーを自動投稿 |
| threads_story | instagram-stories | Threadsの投稿をIGストーリーにも展開 |
| post_saas | threads-bot | SaaSクライアント＋ベモーレ＋個人のThreads投稿（Graph API・主系） |
| generate_posts / saas_generate | threads-bot | Claudeで投稿本文を生成しプールに補充 |
| heartbeat | threads-bot | 投稿が本当に成功したか検証・失敗なら再投稿 |

### ❤️ いいね・コメント
| 名前 | 役割 |
|---|---|
| comment_suggest (Auto Like) | ターゲットアカウントに自動いいね（ベモーレ・個人・Playwright） |
| comment (Auto Comment) | 自動コメント（※現在は通知停止中・異常系のみ通知復活） |

### 🌐 集客・口コミ
| 名前 | 役割 |
|---|---|
| gbp_weekly | GBP週2投稿（Make.com経由で自動投稿・AIO対策） |
| bemolle-review Worker | 口コミ導線（QR→アンケート→AI下書き→投稿／不満はLINE）＋★5自動返信 |
| report_helper | 月次レポート用にIG投稿数を集計（読み取り専用） |

### 🔌 基盤・保守
| 名前 | 役割 |
|---|---|
| cookie_refresh (Mac/launchd) | ThreadsのCookieを自動更新しGitHub Secretに反映 |
| refresh_threads_token | Threads APIトークンの週次更新 |
| research | 反響投稿を分析して投稿ルールを更新 |
| oauth_reminder | SaaSクライアントの連携未完了をリマインド |
| 月次総点検 (ops-audit) | 毎月1日にクラウドAIが全リポをコードレビュー→LINE報告＋2日に見張り番 |
| Square課金監視 (square-reader) | 毎月2日にサブスク課金の完了/失敗をチェック→LINEサマリー |

---

## 3. 🚨 通知が来たら早見表（この通知→この対処）

> 通知は全部「Claude通知Bot」（管理者専用LINE・@047srvio）に届きます。顧客には届きません。

### ❤️ いいね系
| 通知 | 意味 | 対処 |
|---|---|---|
| 🚨 【X】いいねがサーバー側で反映されていません | **本物のMeta制限**（アクション破棄）。3件以上未反映を検知 | **何もしなくてOK**。72時間自動休止→自動再テスト。制限は休むのが唯一の薬 |
| 🚨 【X】自動いいねが停止しました | 連続8件失敗。**ログインは正常**なのでDOM変更/一時障害の疑い | 次の定時実行で自動再試行。連日続けばコード確認（threads_dom.pyのセレクタ） |
| ⚠️ 【X】ログイン判定が4回連続でできていません | ヘッドレスの描画/セッション異常の疑い | セッション確認。連日続けば `playwright_login.py` で再ログイン→Secret更新 |
| 🚨 【X】セッションが未ログイン状態です | Cookie切れが確定 | Chromeの該当プロファイルでThreadsにログイン→cookie_refreshが自動反映（Mac起動時） |

### 📱 投稿系
| 通知 | 意味 | 対処 |
|---|---|---|
| ⚠️ ストーリー失敗 | IG/Threadsストーリー投稿の失敗 | GitHub Actionsのログを確認。写真が無い等が多い |
| 🚨 SaaS投稿: サロン一覧の取得に失敗 | Supabase障害等で全サロン未投稿 | Supabaseの状態を確認 |
| ⚠️ {salon} プール枯渇し過去投稿を再利用 | 投稿ストックが尽きた | saas_generate（月曜）を待つ or 手動実行 |
| ⚠️ {salon} 投稿は成功したが記録保存に失敗 | 投稿はOK・log_post失敗 | 予備cronの再投稿に注意（GBP/Threads確認） |

### 🌐 GBP・口コミ系
| 通知 | 意味 | 対処 |
|---|---|---|
| ⚠️ GBP自動投稿ができませんでした | Makeの実投稿失敗を検知（「posted」応答なし） | **先にGBPを確認**（通信タイムアウト時はMake側で投稿済みの可能性）→無ければLINEの本文を貼る |
| 🟢 ★5口コミに自動返信を投稿しました | 正常動作。**Google投稿が成功した後**に1通だけ届く（2026-07-12変更） | 内容確認だけ。問題あればGBPで修正・削除 |
| （★5が来たのに完了LINEが来ない） | 投稿失敗＝Makeが完了通知に到達していない。Makeからエラーメールが届く | Makeの実行履歴を確認→Replayで再実行 or 手動返信 |
| ⚠️ ★5口コミへの自動返信を保留しました | 安全チェックに引っかかった（要確認） | 生成文を確認して手動で返信 |
| 📣 ご意見フォームに回答 | 不満客のフィードバック | 個別フォロー |

### 🔌 基盤系
| 通知 | 意味 | 対処 |
|---|---|---|
| 🚨 OAuthアカウント不一致を検出 | セッション取り違えの疑い | **重要**：どのアカウントか確認。Chromeプロファイルを確認 |
| ⚠️ Meta access token 期限間近 | IGストーリー用トークンの期限 | トークン更新 |
| Cookie自動更新失敗[X] | cookie_refreshの失敗（GitHub Issueに出る） | Chromeの該当プロファイルで再ログイン |
| 🚨 GASデプロイ版ズレ検知 | GASのコードを保存したが再デプロイし忘れ（古いコードが本番稼働中の疑い） | 通知の該当GASを最新版で再デプロイ（エディタ「デプロイを管理」→鉛筆→バージョン=最新→デプロイ） |
| ⚠️ GASデプロイ版ズレ監視ジョブが失敗/中断 | 監視自体が効いていない可能性 | Claudeに「GAS監視の状態を確認して」と伝える（clasp認証切れ等） |

### 🩺 月次総点検・Square監視系
| 通知 | 意味 | 対処 |
|---|---|---|
| 月次総点検レポート（🟢/🟡/🔴） | 毎月1日の定期便 | 🟢なら何もしない。🟡/🔴ならClaudeに「総点検の指摘直して」と伝える |
| 🚨 月次総点検レポートが届いていません | 点検ルーチン自体の故障 | Claudeに「総点検ルーチンの状態を確認して」と伝える |
| [Square] 課金サマリー | 毎月2日の定期便（進捗と失敗一覧） | 🚨支払い失敗があればお客様にフォロー |
| 🚨 Square課金監視の実行に失敗 | 監視の単発エラー | 同日中の予備実行で自動リトライ。続けて失敗ならActionsを確認 |

---

## 4. よくある復旧手順

### Threadsに再ログインしたい（Cookie切れ時）
1. Mac の Chrome で該当プロファイル（ベモーレ=Profile 4 / 個人=Profile 3）を開く
2. threads.com にログインし直す
3. `cookie_refresh` が次回自動実行時にCookieを抽出→GitHub Secretを更新（手動なら `bash ~/threads_bot/auto_refresh_cookies.sh`）

### いいねの休止を今すぐ解除したい（誤検知だった場合）
1. `threads-bot/like_pause.json`（or `_personal`）を削除
2. commit & push

### 手動でThreads投稿を流したい
- ⚠️ ローカル実行は `session.json`＝ベモーレを使う。実行前にUIDが正しいか確認（取り違え防止）

---

## 5. 状態ファイル リファレンス（何がどこに記録されているか）

| ファイル | 内容 |
|---|---|
| `last_run.json` / `_personal` | 各スロットの最終投稿時刻 |
| `liked_posts.json` / `_personal` | いいね済みアカウント一覧（重複防止） |
| `like_pause.json` / `_personal` | いいね休止中か（制限検知時） |
| `like_alert_state.json` / `_personal` | 通知の重複抑制・破棄カウント・判定不能連続数・最終セッション時刻 |
| `like_verify_queue.json` / `_personal` | 次回反映検証する対象 |
| `posts.json` / `posts_personal.json` / `posts_saas/` | 投稿ストック |
| `used_posts.json` | 使用済み投稿（重複防止） |
| `gbp_state.json` | GBPテーマ輪番・最終投稿日 |
| `used_photos.json`（IG側） | 使用済み写真（連日類似の防止） |

---

## 6. 重要な安全ルール（絶対に守る）

1. **LINE通知の宛先**: 管理者向けは必ず「Claude通知Bot」（1フォロワー）。顧客向けBemolle公式LINE（@344pdkur・505人）には**絶対に流さない**
2. **elme（Lステップ）**: Bemolle=2m69Yg / うらかた=q67o55。bot_idを必ず明示（取り違え=誤配信）
3. **セッション取り違え**: ローカルの session.json は個人アカで上書きされやすい→手動実行前にUID確認
4. **Meta制限は休むのが薬**: 制限中にボットを動かし続けると悪化。休止は正しい動作
5. **秘密情報**: APIキー・トークンはターミナルでのみ扱う。チャットに貼らない

---

*このマニュアルは自動化の変更に合わせて更新してください。最終更新: 2026-07-11*
