# Metaアクション制限 Runbook（threads-bot）

2026-06の両アカウント制限事件の原因分析と、再発防止・回復手順のまとめ。
**このリポジトリの自動化（いいね・コメント）を変更する前に必ず読むこと。**

---

## 1. 何が起きたか（2026-06-01〜06-14）

- 2026-05-29 のパスワード強制変更を機に、Metaが **bemolle_diet / aya_kuroki_0929 両アカの
  ブラウザ自動操作による「書き込み」（コメント）をアクション制限**。
- **投稿（公式API・トークン認証）は無傷**。制限対象はブラウザ自動操作の書き込みのみ。
- 制限の実態は「エラーが出る」ではなく **サイレント破棄**：クリック・送信はすべて成功に
  見えるのに、サーバー側で反映されない（**偽OK**）。これが最も危険な性質で、
  検証なしでは制限に気づけず、叩き続けてフラグが深まる。

### アカウント別の重さ（フラグはアカウント単位・IPではない）

| アカウント | 重さ | 実証された事実 |
|---|---|---|
| 個人 (aya_kuroki_0929) | 軽症 | クリーンセッション＋控えめ運用なら通る。ただしフル稼働(3x/日×3件)で36分で再制限。セッション寿命2-3日→1日へ短縮悪化 |
| bemolle_diet | 重度 | クリーンセッション・住宅IP・実Chrome・headful・stealth・人間挙動を**全部試しても反映ゼロ**。コードでは突破不可＝休養のみが解 |

## 2. 原因の結論

1. **トリガー**：パスワード強制変更（Meta側がアカウントを「要監視」に格上げ）
2. **判定対象**：ブラウザ自動操作（Playwright）による**書き込みアクション**。
   自動化指紋はIP/ブラウザ/stealthを変えても検出される（手動の本物アプリは通る）
3. **増悪要因**：制限中・復活直後のフル稼働（高頻度の自動アクション）

## 3. 再発防止ルール（絶対）

1. **書き込み自動化には必ず反映検証をつける**（クリック成功≠成功）。
   - いいね: `like_auto.py` の canary（前回いいねを次回冒頭で再訪問確認）
   - コメント: `_verify_comment_posted`（投稿ページ＋/repliesタブの2段確認）
2. **異常検知後は自動休止**（cronが制限中のアカを叩き続けない）。
   - いいね: サイレント破棄検知で72h自動休止（`like_pause*.json`）→自動再テスト
3. **復活直後は控えめから**。いきなりフル稼働しない（個人アカが36分で再制限された実績）。
   目安: 1日1回×少数 → 数日問題なければ段階的に増やす
4. **パスワード変更・セキュリティイベントの直後は自動化を数日止める**（フラグ格上げの引き金）
5. **制限中の外部リンク投稿は厳禁**（スパム判定を深める）
6. **警告通知は複数経路の確認後・単発で**（検証が不安定だと誤報→誤った大方針につながる）

## 4. 現在の構成（2026-07-03時点）

| 機能 | 状態 | 経路 |
|---|---|---|
| 投稿 | ✅ 全アカ稼働 | 公式API（トークン）＝制限と無関係 |
| いいね | ✅ 稼働（7/2再開） | GH Actions・5件×3回/日・反映検証＋自動休止つき |
| コメント自動 | ⛔ 停止 | comment.yml で bemolle=false / personal=false（手動dispatchのみ可） |
| コメント提案(suggest) | 廃止（7/2） | LINE枠節約のため。comment_suggest.yml はLike専用に転用 |

## 5. 症状別の対処（runbook）

### 🚨「いいねがサーバー側で反映されていません」LINE
= サイレント破棄＝アクション制限の再発。**操作不要**（72h自動休止→自動再テスト）。
繰り返す場合はいいねも長期休養へ：cron-job.orgのcomment-*発火を止めるか、
`like_pause*.json` の `until` を手動で延長。

### 🚨「自動いいねが停止しました（連続8件失敗）」LINE
= セッション(Cookie)切れの可能性大。
```
cd ~/projects/threads-bot
python3 playwright_login.py bemolle   # または personal
gh secret set THREADS_SESSION < session.json   # personalは THREADS_SESSION_PERSONAL
```
更新すれば次の定時実行から自動復帰。休止はかからない。

### コメント自動を再開したくなったら
1. まず**手動で1件**コメントし、反映されるか確認（ログアウト状態でも見えるか）
2. 次に `gh workflow run comment.yml -f account=bemolle -f dry_run=false` で自動1件→反映確認
3. 通ったら控えめ設定（1日1回×1件）から。comment.yml のフラグを戻すのは最後
4. bemolleはローカルMac(住宅IP)実行基盤が待機中: `~/threads_bot/comment_local_bemolle.sh`
   （launchd `com.threads_bot.comment_bemolle`・現在unload+.disabled）

### 投稿が止まったら
制限とは別問題（公式APIはトークン期限・レート）。`refresh_threads_token.yml` と
heartbeat のログを確認。

## 6. 教訓（2026-06事件から）

1. Metaのアクション制限はIP/ブラウザ/stealthで突破不可。重度フラグは休養のみ
2. 自動が無理でも「AIが探して書く→人間が送る」半自動で価値は出せる
3. 復活直後のフル稼働は即再制限。控えめから
4. 二重投稿防止はアカウント単位では不十分。投稿ID＋実返信存在チェック
5. 投稿とコメントは認証が別物（APIトークン vs ブラウザCookie）
6. 「成功の検証」が不安定だと誤報が誤った大方針につながる。複数経路確認＋警告は単発で
7. GitHub内蔵scheduleは遅延/スキップが多い。確実な定時はcron-job.orgのrepository_dispatch
8. AIコメント生成は本人属性の捏造に注意（年齢・職業・家族はNGルール明示）
